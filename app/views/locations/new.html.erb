<!DOCTYPE html>
<html>
  <head>
    <title>Adventure</title>

  </head>

  <body>
    <h1>New Location</h1>
<%= form_for(@location) do |f| %>
<%= f.label(:name) %>
<%= f.text_field(:name) %>

<br />
<%= f.submit %>
<% end %>
  </body>
</html>
